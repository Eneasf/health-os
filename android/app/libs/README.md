# Samsung Health Data SDK Library Directory

Place the `samsung-health-data-api-1.1.0.aar` binary downloaded from the Samsung Developer Portal in this directory.

The Gradle build automatically links all `*.aar` and `*.jar` binaries located in `app/libs/`.
